import {
     ErButton,
     ErIcon,
     ErButtonGroup,
     ErCollapse,
     ErCollapseItem,
     ErAlert,
     ErTooltip
     } from "@toy-element/components";
import type {Plugin} from 'vue'

export default[
    ErButton,
    ErIcon,
    ErButtonGroup,
    ErCollapseItem,
    ErCollapse,
    ErAlert,
    ErTooltip
]as Plugin[];
  
